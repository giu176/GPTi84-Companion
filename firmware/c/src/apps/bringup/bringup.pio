; PIO programs for bring-up tests.
; pioasm auto-generates `<program>_program_get_default_config(offset)`

.program hold_high
    set pins, 1
.wrap_target
    nop
.wrap


.program shift_out
.wrap_target
    out pins, 1 [1]
.wrap
