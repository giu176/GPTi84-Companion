# Copy to secrets.py (gitignored). BLE is the production phone transport.

# --- BLE production transport ---------------------------------------------

RELAY_TRANSPORT = "ble"
BLE_DEVICE_NAME = "GPTi84-Pico"
BLE_ECHO_MODE = False
RAW_AXE_MOCK_MODE = False

# Reserved for device-bound pairing. Leave blank for development bring-up;
# the app and Pico will persist a generated secret after pairing is enabled.
BLE_PAIRING_SECRET_SEED = ""
BLE_DEV_RESET_PAIRING = False

# --- Temporary TCP development transport ----------------------------------
#
# Set RELAY_TRANSPORT = "tcp" only for Android/LAN diagnostics.

WIFI_SSID = ""
WIFI_PASSWORD = ""
SERVER_HOST = "192.168.1.1"
SERVER_PORT = 8784
